services:
  homeassistant:
    container_name: homeassistant-custom-version
    image: ghcr.io/home-assistant/home-assistant:2025.4
    privileged: true
    network_mode: host
    environment:
       PYTHONPATH: /config/deps/
    volumes:
      - ../custom_components/spotcast:/config/custom_components/spotcast
      - ../config/:/config/

